# On A Trip Holidays — SEO V18

## V18 focus
Technical SEO cleanup + easier full-repository deployment.

### Website changes
- Cleaned malformed image markup introduced by earlier visual upgrades.
- Kept destination photography, premium landing-page design, FAQs, CTAs, canonical URLs and existing schema.
- Removed legacy redirect URLs from `sitemap.xml`; the sitemap should contain canonical, indexable destinations rather than URLs that permanently redirect.
- Added `lastmod` dates for the main Hyderabad destination landing pages updated in V18.
- Preserved V14/V17 redirects and noindex rules.
- Added a simple deployment guide for updating the whole GitHub repository in one operation with GitHub Desktop or a local Git clone.

## Important
V18 does not invent current prices, batch dates, seat counts or availability. Confirm live commercial details before publishing them.
