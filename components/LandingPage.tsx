import Link from "next/link"; import SiteShell from "./SiteShell"; import PackageCard from "./PackageCard"; import {packageBySlug} from "@/lib/site-data";
export default function LandingPage({title,description,slugs}:{title:string;description:string;slugs:string[]}){const items=slugs.map(packageBySlug).filter(Boolean);return <SiteShell><main><section className="page-hero"><div className="container"><span className="eyebrow">TRAVEL FROM HYDERABAD</span><h1>{title}</h1><p>{description}</p><Link className="btn primary" href="/contact">Get a custom quote →</Link></div></section><section className="section"><div className="container"><div className="package-grid">{items.map(x=><PackageCard key={x!.slug} item={x!}/>)}</div><div className="callout"><div><b>Need a different route?</b><p>We can customize dates, hotel category, vehicle and sightseeing around your group.</p></div><Link className="btn primary" href="/contact">Talk to our team →</Link></div></div></section></main></SiteShell>}
