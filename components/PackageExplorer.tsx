"use client"; import {useMemo,useState} from "react"; import type {Package} from "@/lib/site-data"; import PackageCard from "./PackageCard";
export default function PackageExplorer({items}:{items:Package[]}){const[q,setQ]=useState("");const[cat,setCat]=useState("All");const filtered=useMemo(()=>items.filter(x=>(cat==="All"||x.category===cat)&&`${x.name} ${x.route} ${x.from}`.toLowerCase().includes(q.toLowerCase())),[items,q,cat]);return <><div className="filters"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search destination or package…"/><select value={cat} onChange={e=>setCat(e.target.value)}><option>All</option><option>Pilgrimage</option><option>Domestic</option><option>International</option></select><span>{filtered.length} packages</span></div><div className="package-grid">{filtered.map(x=><PackageCard key={x.slug} item={x}/>)}</div>{!filtered.length&&<div className="empty">No package matches that search. Try another destination or <a href="/contact">request a custom trip</a>.</div>}</>}
