import type {Metadata} from "next"; import LandingPage from "@/components/LandingPage";
export const metadata:Metadata={title:"Kashmir Tour Packages from Hyderabad",description:"Explore Kashmir holidays with flexible group and private options."};
export default function Page(){return <LandingPage title="Kashmir Tour Packages from Hyderabad" description="Explore Kashmir holidays with flexible group and private options." slugs={["kashmir"]}/>}
