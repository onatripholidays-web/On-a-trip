import type {Metadata} from "next"; import SiteShell from "@/components/SiteShell"; import PackageExplorer from "@/components/PackageExplorer"; import {packages} from "@/lib/site-data"; import {getCmsPackages} from "@/lib/cms";
export const metadata:Metadata={title:"Tour Packages",description:"Explore pilgrimage, domestic and international tour packages from On A Trip Holidays."};
export const revalidate=60;
export default async function Packages(){const cms=await getCmsPackages();const items=cms||packages;return <SiteShell><main><section className="page-hero"><div className="container"><span className="eyebrow">OUR COLLECTION</span><h1>Find your <em>next trip.</em></h1><p>Browse the current On A Trip Holidays collection. Search by destination or filter by travel style.</p></div></section><section className="section"><div className="container"><PackageExplorer items={items}/></div></section></main></SiteShell>}
