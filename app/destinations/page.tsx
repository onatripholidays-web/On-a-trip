import type {Metadata} from "next"; import Link from "next/link"; import SiteShell from "@/components/SiteShell"; import {destinationMeta} from "@/lib/site-data";
export const metadata:Metadata={title:"Destinations",description:"Explore mountain, pilgrimage, Kerala and international destinations with On A Trip Holidays."};
export default function Destinations(){return <SiteShell><main><section className="page-hero"><div className="container"><span className="eyebrow">EXPLORE BY STYLE</span><h1>Choose the place. <em>We’ll plan the journey.</em></h1><p>Focused destination collections, without confusing or unrelated packages.</p></div></section><section className="section"><div className="container destination-grid-large">{Object.entries(destinationMeta).map(([slug,d])=><Link className="destination-tile" href={`/destinations/${slug}`} key={slug}><div className={`destination-visual ${slug}`}/><div><span className="eyebrow">{d.name}</span><h2>{d.name}</h2><p>{d.description}</p><b>Explore collection →</b></div></Link>)}</div></section></main></SiteShell>}
