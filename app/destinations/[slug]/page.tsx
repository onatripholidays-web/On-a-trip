import type {Metadata} from "next"; import {notFound} from "next/navigation"; import Link from "next/link"; import SiteShell from "@/components/SiteShell"; import PackageCard from "@/components/PackageCard"; import {destinationMeta,packageBySlug} from "@/lib/site-data";
export async function generateStaticParams(){return Object.keys(destinationMeta).map(slug=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const d=destinationMeta[slug as keyof typeof destinationMeta];return d?{title:`${d.name} Travel Packages`,description:d.description}:{}};
export default async function Destination({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const d=destinationMeta[slug as keyof typeof destinationMeta];if(!d)notFound();const items=d.packages.map(packageBySlug).filter(Boolean);return <SiteShell><main><section className="page-hero"><div className="container"><span className="eyebrow">DESTINATION COLLECTION</span><h1>{d.name} <em>holidays.</em></h1><p>{d.description}</p></div></section><section className="section"><div className="container"><div className="package-grid">{items.map(x=><PackageCard key={x!.slug} item={x!}/>)}</div><div className="callout"><div><b>Need something different?</b><p>Tell us your dates, departure city and budget and we’ll build a custom option.</p></div><Link className="btn primary" href="/contact">Request a custom trip →</Link></div></div></section></main></SiteShell>}
