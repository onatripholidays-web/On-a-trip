import {NextResponse} from "next/server";
import {cookies} from "next/headers";
import {getCrmSession,crmSupabaseConfig} from "@/lib/crm-auth";
function h(c:any,extra:any={}){return {apikey:c.key,Authorization:`Bearer ${c.token}`,...extra}}
async function ctx(){const session=await getCrmSession();if(!session)return null;const token=(await cookies()).get("oat_crm_access")?.value||"";const {url,key}=crmSupabaseConfig();return {session,token,url,key}}
export async function POST(req:Request){const c=await ctx();if(!c)return NextResponse.json({error:"Unauthorized"},{status:401});let b:any=null;try{b=await req.json()}catch{b=null}if(!b?.customer?.name||!b?.customer?.phone||!b?.customer?.destination)return NextResponse.json({error:"Customer name, phone and destination are required"},{status:400});try{const base={name:String(b.customer.name).trim(),phone:String(b.customer.phone).trim(),email:b.customer.email||null,destination:b.customer.destination,source:"CRM Quotation",assigned_to:c.session.profile.salesperson||null,created_by:c.session.user.id};const cr=await fetch(`${c.url}/rest/v1/crm_customers?on_conflict=phone`,{method:"POST",headers:h(c,{"Content-Type":"application/json",Prefer:"resolution=merge-duplicates,return=representation"}),body:JSON.stringify(base)});let cd:any=null;try{cd=await cr.json()}catch{cd=null}if(!cr.ok)throw new Error("Customer save failed");const customer=Array.isArray(cd)?cd[0]:cd;const rawItems=Array.isArray(b.items)?b.items:[];const items=rawItems.map((x:any,i:number)=>{const quantity=Number(x.quantity)||1;const cost=Number(x.unit_cost)||0;const markup=Number(x.markup)||0;const taxRate=Number(x.tax_rate)||0;const sell=quantity*(cost+markup);return {quotation_id:"",item_type:"service",description:x.description||"Service",supplier:x.supplier||null,quantity,unit_cost:cost,markup,tax_rate:taxRate,sell_price:sell,sort_order:i,_tax:sell*taxRate/100}});const subtotal=items.reduce((s:any,x:any)=>s+x.sell_price,0);const tax=items.reduce((s:any,x:any)=>s+x._tax,0);const discount=Math.max(0,Number(b.discount)||0);const tcs=Math.max(0,Number(b.tcs)||0);const total=Math.max(0,subtotal+tax-discount+tcs);const qno=`QT-${new Date().toISOString().slice(0,7).replace("-","")}-${crypto.randomUUID().slice(0,6).toUpperCase()}`;const emptyList:string[]=[];const q={quotation_no:qno,customer_id:customer?.id||null,title:b.customer.title||"Holiday Package",destination:b.customer.destination,travel_date:b.customer.travel_date||null,return_date:b.customer.return_date||null,travellers:Number(b.customer.travellers)||1,subtotal,discount,tax,tcs,total,status:"draft",valid_until:b.valid_until||null,terms:b.terms||null,created_by:c.session.user.id,itinerary:emptyList};const qr=await fetch(`${c.url}/rest/v1/crm_quotations`,{method:"POST",headers:h(c,{"Content-Type":"application/json",Prefer:"return=representation"}),body:JSON.stringify(q)});let qd:any=null;try{qd=await qr.json()}catch{qd=null}if(!qr.ok)throw new Error("Quotation save failed");const quotation=Array.isArray(qd)?qd[0]:qd;const payload=items.map((x:any)=>{const y={...x};delete y._tax;y.quotation_id=quotation.id;return y});if(payload.length){const ir=await fetch(`${c.url}/rest/v1/crm_quotation_items`,{method:"POST",headers:h(c,{"Content-Type":"application/json",Prefer:"return=minimal"}),body:JSON.stringify(payload)});if(!ir.ok)throw new Error("Quotation items save failed")}return NextResponse.json({ok:true,quotation,customer})}catch(e:any){return NextResponse.json({error:e?.message||"Could not save quotation"},{status:502})}}
