import {NextResponse} from "next/server";
import {cookies} from "next/headers";
import {getCrmSession,crmSupabaseConfig} from "@/lib/crm-auth";
async function ctx(){const session=await getCrmSession();if(!session)return null;const token=(await cookies()).get("oat_crm_access")?.value||"";const {url,key}=crmSupabaseConfig();return {session,token,url,key}}
function h(c:any,extra:any={}){return {apikey:c.key,Authorization:`Bearer ${c.token}`,...extra}}
export async function POST(req:Request){const c=await ctx();if(!c)return NextResponse.json({error:"Unauthorized"},{status:401});const {quotation_id}=await req.json().catch(()=>({}));if(!quotation_id)return NextResponse.json({error:"quotation_id is required"},{status:400});try{const qR=await fetch(`${c.url}/rest/v1/crm_quotations?id=eq.${encodeURIComponent(quotation_id)}&select=*`,{headers:h(c),cache:"no-store"});const qs=await qR.json();const q=Array.isArray(qs)?qs[0]:null;if(!q)return NextResponse.json({error:"Quotation not found"},{status:404});const bno=`BK-${new Date().toISOString().slice(0,7).replace("-","")}-${crypto.randomUUID().slice(0,6).toUpperCase()}`;const booking={booking_no:bno,quotation_id:q.id,customer_id:q.customer_id,enquiry_id:q.enquiry_id||null,destination:q.destination,departure_date:q.travel_date||null,return_date:q.return_date||null,travellers:q.travellers||1,total_amount:q.total||0,paid_amount:0,balance_amount:q.total||0,status:"confirmed",assigned_to:c.session.profile.salesperson||null,created_by:c.session.user.id};const r=await fetch(`${c.url}/rest/v1/crm_bookings`,{method:"POST",headers:h(c,{"Content-Type":"application/json",Prefer:"return=representation"}),body:JSON.stringify(booking)});const d=await r.json().catch(()=>null);if(!r.ok)return NextResponse.json({error:"Could not create booking",details:d},{status:502});const bookingRow=Array.isArray(d)?d[0]:d;await fetch(`${c.url}/rest/v1/crm_quotations?id=eq.${encodeURIComponent(q.id)}`,{method:"PATCH",headers:h(c,{"Content-Type":"application/json",Prefer:"return=minimal"}),body:JSON.stringify({status:"converted"})});if(q.enquiry_id)await fetch(`${c.url}/rest/v1/enquiries?id=eq.${encodeURIComponent(q.enquiry_id)}`,{method:"PATCH",headers:h(c,{"Content-Type":"application/json",Prefer:"return=minimal"}),body:JSON.stringify({status:"Booked"})});return NextResponse.json({ok:true,booking:bookingRow})}catch(e:any){return NextResponse.json({error:e?.message||"Conversion failed"},{status:502})}}
