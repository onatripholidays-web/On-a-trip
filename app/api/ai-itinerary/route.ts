import {NextResponse} from "next/server";
import {z} from "zod";
import {getCrmSession} from "@/lib/crm-auth";

export const dynamic="force-dynamic";
const inputSchema=z.object({guest:z.string().trim().max(120).optional().default("Guest"),dest:z.string().trim().min(2).max(120),days:z.union([z.string(),z.number()]).transform(Number).pipe(z.number().int().min(1).max(30)),pax:z.union([z.string(),z.number()]).transform(Number).pipe(z.number().int().min(1).max(100)),start:z.string().trim().max(120).optional().default("TBC"),dates:z.string().trim().max(120).optional().default("TBC"),language:z.string().trim().max(40).optional().default("English"),type:z.string().trim().max(40).optional().default("Custom"),hotel:z.string().trim().max(120).optional().default("3 Star"),vehicle:z.string().trim().max(120).optional().default("Private Car"),meals:z.string().trim().max(120).optional().default("Breakfast & Dinner"),budget:z.string().trim().max(200).optional().default("Price to be quoted"),special:z.string().trim().max(1000).optional().default("None"),instruction:z.string().trim().max(1000).optional().default("None")});
async function authorized(){return Boolean(await getCrmSession())}
export async function GET(){if(!(await authorized()))return NextResponse.json({error:"Unauthorized"},{status:401});return NextResponse.json({ok:true,service:"On A Trip Holidays AI Itinerary",configured:Boolean(process.env.OPENAI_API_KEY)});}
export async function POST(req:Request){if(!(await authorized()))return NextResponse.json({error:"Unauthorized"},{status:401});const apiKey=process.env.OPENAI_API_KEY;if(!apiKey)return NextResponse.json({error:"OPENAI_API_KEY is not configured for this deployment."},{status:503});try{const parsed=inputSchema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:"Please check the itinerary details."},{status:400});const r=parsed.data;const days=r.days;const pax=r.pax;const system=`You are the senior itinerary planner for On A Trip Holidays, a Telugu-first travel company in India. Create a professional customer-ready itinerary. The destination must be real and specific. Never use undefined, blank, Custom Trip or generic filler. Respect exactly the requested number of days. Never invent confirmed bookings, permits, prices, availability or darshan slots; use subject to confirmation when needed. Preserve supplied quote exactly. For pilgrimage trips use respectful sequencing and senior-friendly pacing. For high-altitude/adventure trips include acclimatization, realistic drives and safety buffers. Match the requested language. Output only valid JSON matching the schema.`;const user=`Guest: ${r.guest}\nDestination: ${r.dest}\nTravellers: ${pax}\nStart city: ${r.start}\nTravel dates: ${r.dates}\nLanguage: ${r.language}\nTrip type: ${r.type}\nHotel: ${r.hotel}\nVehicle: ${r.vehicle}\nMeals: ${r.meals}\nBudget/quote: ${r.budget}\nSpecial requirements: ${r.special}\nInstruction: ${r.instruction}\nCreate exactly ${days} days.`;const schema={type:"object",additionalProperties:false,required:["title","summary","days","hotels","vehicle","meals","price","inclusions","exclusions","notes"],properties:{title:{type:"string"},summary:{type:"string"},days:{type:"array",minItems:days,maxItems:days,items:{type:"object",additionalProperties:false,required:["day","title","summary","hotel","meals"],properties:{day:{type:"integer"},title:{type:"string"},summary:{type:"string"},hotel:{type:"string"},meals:{type:"string"}}}},hotels:{type:"string"},vehicle:{type:"string"},meals:{type:"string"},price:{type:"string"},inclusions:{type:"array",items:{type:"string"}},exclusions:{type:"array",items:{type:"string"}},notes:{type:"string"}}};const response=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${apiKey}`},body:JSON.stringify({model:process.env.OPENAI_MODEL||"gpt-5.6-luna",input:[{role:"system",content:[{type:"input_text",text:system}]},{role:"user",content:[{type:"input_text",text:user}]}],text:{format:{type:"json_schema",name:"on_a_trip_itinerary",strict:true,schema}}})});const raw=await response.text();if(!response.ok){let detail="OpenAI request failed.";try{detail=JSON.parse(raw)?.error?.message||detail}catch{}return NextResponse.json({error:`AI provider error (${response.status}): ${detail}`},{status:502});}const data=JSON.parse(raw);let text=typeof data.output_text==="string"?data.output_text:"";if(!text&&Array.isArray(data.output))for(const item of data.output){for(const c of item?.content||[]){if(typeof c?.text==="string"&&c.text.trim()){text=c.text;break;}}if(text)break;}if(!text)return NextResponse.json({error:"AI returned no itinerary text."},{status:502});const itinerary=JSON.parse(text);if(!itinerary.title||!Array.isArray(itinerary.days)||itinerary.days.length!==days)return NextResponse.json({error:"AI returned an incomplete itinerary."},{status:502});return NextResponse.json(itinerary);}catch(error){console.error(error);return NextResponse.json({error:"Unable to generate itinerary. Please try again."},{status:500});}}
