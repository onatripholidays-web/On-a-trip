import type {Metadata} from "next"; import SiteShell from "@/components/SiteShell"; import PackageCard from "@/components/PackageCard"; import {packages} from "@/lib/site-data";
export const metadata:Metadata={title:"On A Trip Temple | Pilgrimage Tours",description:"Char Dham, Do Dham and Kedarnath pilgrimage packages with Telugu-first travel support."};
export default function Pilgrimage(){const items=packages.filter(x=>x.category==="Pilgrimage");return <SiteShell><main><section className="page-hero pilgrimage-hero"><div className="container"><span className="eyebrow">ON A TRIP TEMPLE</span><h1>Travel with <em>faith.</em></h1><p>Dedicated pilgrimage journeys for Telugu travellers, with clear itineraries, practical support and coordinated stays and transport.</p></div></section><section className="section"><div className="container"><div className="package-grid">{items.map(x=><PackageCard key={x.slug} item={x}/>)}</div></div></section></main></SiteShell>}
